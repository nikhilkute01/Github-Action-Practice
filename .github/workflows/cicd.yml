name: CICD

on: 
  workflow_dispatch:


jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: Clone the code
        run: echo "Cloning"
  build:
    needs: code
    runs-on: ubuntu-latest
    steps:
      - name: Building using docker
        run: echo "Building docker"

  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Runnning tst case
        run: echo "Ruuning test"


  deploy:
    needs: [build,test]
    runs-on: ubuntu-latest
    steps:
      - name: Deployinhg machine
        run: echo "DEPLOYING"
